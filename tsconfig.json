{
  "compilerOptions": {
    "target": "ES2022",                   // Garante recursos modernos (async/await, top-level await, etc.)
    "module": "es2022",                 // Usa sistema de módulos ESM
    "moduleResolution": "bundler",       // Resolve conforme o padrão Node + ESM
    "lib": ["ES2022"],                    // Usa APIs modernas do JS
    "rootDir": "src",                     // Código fonte
    "outDir": "dist",                     // Onde vão os arquivos .js compilados
    "resolveJsonModule": true,            // Permite importar JSON
    "esModuleInterop": true,              // Compatibilidade com módulos CommonJS
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,                 // Evita erros de tipagem em node_modules
    "strict": true,                       // Ativa todas as verificações de tipo
    "noImplicitAny": true,                // Exige tipagem explícita
    "noUnusedLocals": true,               // Remove variáveis não usadas
    "noUnusedParameters": true            // Remove parâmetros não usados
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}